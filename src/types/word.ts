export interface WordRow {
    key: number | string;
    chinese: string;
    hanViet: string;
    pinyin: string;
    vietnamese: string;
}

// Thêm các type/interface khác nếu cần ở đây
export { };

